#!/usr/bin/make -f
export DH_VERBOSE = 1
export PYBUILD_NAME = clipboard-manager

%:
	dh $@ --with python3 --buildsystem=pybuild

override_dh_auto_test:
	# tests run separately in CI via pytest, not during packaging
